const publications = require('../exemple/publications');

module.exports = (app) => 
{
    app.get('/TySyNday/pubAccueil/',(req,res) => 
    {
        const id = req.params.id;
        /*for(let pub of publications)
        {
            if()
        }*/
    })
}